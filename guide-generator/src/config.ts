// Centralized branding configuration
// Set VITE_BRAND_URL in your environment to override the default URL.
export const BRAND_URL: string = (import.meta as any)?.env?.VITE_BRAND_URL || 'https://guide-airbnb.vercel.app';
